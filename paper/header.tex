% header.tex — xelatex font configuration for empirical_cdcc_paper.md
% Loaded via pandoc --include-in-header=paper/header.tex

\usepackage{fontspec}
\usepackage{unicode-math}
\usepackage{dirtree}  % directory-tree diagrams — used in §4.1

% Main body font: DejaVu Serif (covers Greek, math, and common Unicode)
\setmainfont{DejaVu Serif}

% Monospaced font: DejaVu Sans Mono with Noto Emoji as emoji fallback
\setmonofont{DejaVu Sans Mono}[
  Scale=0.85,
  BoldFont={DejaVu Sans Mono Bold},
  ItalicFont={DejaVu Sans Mono Oblique},
  FallbackFonts={Noto Emoji}
]

% Sans-serif for headings
\setsansfont{DejaVu Sans}

% Avoid overfull hbox warnings for long identifiers in code blocks
\setlength{\emergencystretch}{3em}
